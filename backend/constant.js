const NO_RECORD_AVAILABLE = "No data avalable to validate"
const END_BALANCE_ISSUE = 'There are End Balance count mismatch';
const DUPLICATE_RECORDS = 'There are duplicate transaction exist';

module.exports = {NO_RECORD_AVAILABLE,END_BALANCE_ISSUE,DUPLICATE_RECORDS }